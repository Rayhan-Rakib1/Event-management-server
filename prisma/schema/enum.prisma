enum Role {
    USER
    HOST
    ADMIN
}

enum EventStatus {
    OPEN
    FULL
    CANCELLED
    COMPLETED
}

enum PaymentStatus {
    UNPAID
    PAID
    FAILED
    REFUNDED
}

enum ParticipantStatus {
    JOINED
    LEFT
    CANCELLED
}

enum UserStatus {
    ACTIVE
    INACTIVE
    DELETED
}

enum Gender {
    MALE
    FEMALE
}
